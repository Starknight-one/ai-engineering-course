# Task Tracker - Аналог Jira

Простое приложение для управления задачами с возможностью drag-and-drop перемещения между статусами.

## Возможности

- ✅ Создание задач с названием и описанием
- ✅ Перетаскивание задач между статусами (Todo → In Progress → Test → Done)
- ✅ Редактирование содержимого задач
- ✅ Удаление задач
- ✅ Drag-and-drop интерфейс
- ✅ REST API для управления задачами

## Технологии

### Frontend
- React 18 + TypeScript
- Vite (сборщик)
- @dnd-kit (drag-and-drop)
- CSS Modules

### Backend
- Node.js + Express + TypeScript
- JSON файл для хранения данных
- REST API
- CORS поддержка

## Структура проекта

```
.
├── app/                    # Основная директория приложения
│   ├── frontend/          # React приложение
│   │   ├── src/
│   │   │   ├── components/   # Компоненты (Board, Column, TaskCard)
│   │   │   ├── types/        # TypeScript типы
│   │   │   └── App.tsx       # Главный компонент
│   │   └── package.json
│   │
│   ├── backend/           # Express API сервер
│   │   ├── src/
│   │   │   ├── routes/       # API маршруты
│   │   │   ├── types/        # TypeScript типы
│   │   │   ├── data/         # tasks.json для хранения
│   │   │   └── index.ts      # Точка входа
│   │   └── package.json
│   │
│   ├── start.sh           # Скрипт запуска приложения
│   ├── stop.sh            # Скрипт остановки приложения
│   ├── logs/              # Логи frontend и backend
│   └── .pids/             # PID файлы процессов
│
├── ADW/                   # Automation & Development Workflows
│   └── README.md          # Python скрипты для автоматизации
│
├── .claude/               # Claude Code конфигурация
│   └── commands/          # Пользовательские команды
│       ├── tool.md        # /tool - список встроенных инструментов
│       └── prime.md       # /prime - быстрое знакомство с проектом
│
└── README.md              # Этот файл
```

## Быстрый старт

### Автоматический запуск (рекомендуется)

Используйте готовые скрипты для запуска и остановки приложения:

```bash
# Переход в директорию приложения
cd app

# Запуск frontend и backend
./start.sh

# Остановка всех процессов
./stop.sh
```

Скрипт `start.sh` автоматически:
- Проверит и установит зависимости (если нужно)
- Запустит backend на `http://localhost:3001`
- Запустит frontend на `http://localhost:3000`
- Сохранит логи в `logs/backend.log` и `logs/frontend.log`

### Ручной запуск

Если хотите запустить вручную, откройте два терминала:

**Терминал 1 - Backend:**
```bash
cd app/backend
npm install  # первый раз
npm run dev
```

**Терминал 2 - Frontend:**
```bash
cd app/frontend
npm install  # первый раз
npm run dev
```

### Просмотр логов

```bash
# В реальном времени
tail -f app/logs/backend.log
tail -f app/logs/frontend.log
```

## Использование

1. Откройте браузер и перейдите на `http://localhost:3000`
2. Нажмите "Add Task" для создания новой задачи
3. Заполните название и описание
4. Перетаскивайте задачи между колонками (Todo, In Progress, Test, Done)
5. Удаляйте задачи кнопкой "×"

## API Endpoints

### Tasks API

- `GET /api/tasks` - Получить все задачи
- `POST /api/tasks` - Создать новую задачу
- `PUT /api/tasks/:id` - Обновить задачу (включая status)
- `DELETE /api/tasks/:id` - Удалить задачу

### Пример запроса

```bash
# Создать задачу
curl -X POST http://localhost:3001/api/tasks \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Моя задача",
    "description": "Описание задачи",
    "status": "todo"
  }'

# Обновить статус
curl -X PUT http://localhost:3001/api/tasks/{id} \
  -H "Content-Type: application/json" \
  -d '{
    "status": "in-progress"
  }'
```

## Структура данных

### Task

```typescript
interface Task {
  id: string;           // Уникальный идентификатор
  title: string;        // Название задачи
  description: string;  // Описание
  status: 'todo' | 'in-progress' | 'test' | 'done';  // Статус
  createdAt: string;    // Дата создания
  updatedAt: string;    // Дата обновления
}
```

## Разработка

### Frontend

```bash
cd app/frontend
npm run dev      # Запуск dev сервера
npm run build    # Сборка для продакшена
npm run preview  # Превью production сборки
```

### Backend

```bash
cd app/backend
npm run dev      # Запуск с автоперезагрузкой
npm run build    # Компиляция TypeScript
npm start        # Запуск скомпилированного кода
```

## Хранение данных

Данные хранятся в файле `app/backend/src/data/tasks.json`. При первом запуске создаются примеры задач. Все изменения сохраняются в этот файл автоматически.

## Автоматизация (ADW)

Директория `ADW/` предназначена для Python скриптов автоматизации:
- Запуск последовательностей команд Claude Code
- Автоматизация повторяющихся задач
- Интеграция с внешними API
- Batch операции над проектом

Подробнее см. `ADW/README.md`

## Claude Code команды

Проект включает пользовательские команды для Claude Code:

| Команда | Описание |
|---------|----------|
| `/prime` | Быстрое знакомство с проектом — выводит структуру файлов и README |
| `/tool` | Показывает список встроенных инструментов Claude Code |

Команды расположены в `.claude/commands/`

## Следующие шаги

Базовый скелет готов! Можно добавить:

- 🔄 Подключение frontend к backend API
- 💾 Персистентность при обновлении страницы
- 🎨 Улучшенный UI/UX
- 🔍 Поиск и фильтрация задач
- 🏷️ Теги и категории
- 📅 Дедлайны и приоритеты
- 👥 Назначение исполнителей
- 📱 Адаптивный дизайн для мобильных

## Лицензия

MIT
